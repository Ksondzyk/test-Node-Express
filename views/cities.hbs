<h1>Cities</h1>
{{#if cities.length}}
{{#each cities}}
<div class="row">
  <div class="col s6 offset-s3">
    <div class="card">
      <div class="card-image">
        <img src="{{image}}" alt="{{title}}">
      </div>
      <div class="card-content">
        <span class="card-title">{{title}}</span>
      </div>
      <div class="card-content">
        <span class="card-title">{{numberOfResidents}} Citizen</span>
      </div>
      <div class="card-action">
        <a href="/cities/{{id}}" target="_blank">Посмотреть город</a>
      </div>
    </div>
  </div>
</div>
{{/each}}
{{else}}
<p>Городов Нет</p>
{{/if}}